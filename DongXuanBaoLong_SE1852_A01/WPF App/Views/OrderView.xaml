<UserControl x:Class="DongXuanBaoLongWPF.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DongXuanBaoLongWPF.Views"
             xmlns:vm="clr-namespace:DongXuanBaoLongWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <!-- Order Summary Table -->
        <GroupBox Header="Orders" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding Orders}"
                      SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}" Width="*" />
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*" />
                    <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeName}" Width="*" />
                    <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Product Items of Selected Order -->
        <GroupBox Header="Order Items" FontWeight="Bold" Grid.Row="1" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding SelectedOrderItems}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product ID" Binding="{Binding ProductID}" Width="*" />
                    <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="2*" />
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*" />
                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" Width="*" />
                    <DataGridTextColumn Header="Discount" Binding="{Binding Discount}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Product Search + Selection -->
        <GroupBox Header="Search and Select Product" FontWeight="Bold" Grid.Row="2">
            <StackPanel Margin="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Search Product:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Width="200" 
                             Text="{Binding ProductSearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Content="Search" Width="80" Margin="10,0,0,0" Click="SearchProduct_Click" />
                </StackPanel>

                <ListBox ItemsSource="{Binding Products}"
                         SelectedItem="{Binding SelectedProduct}"
                         DisplayMemberPath="ProductName"
                         Height="120"
                         Margin="0,10,0,0"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
